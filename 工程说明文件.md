# dsh-advisor-group — 中文说明

> 整理：agent ｜ 状态：**单机可用**（本机 127.0.0.1 / 单用户场景已验证；多用户或局域网暴露仍需外层鉴权与成本治理）｜ 2026-09-04 已适配 **DSH 0.1.2-rc.1**（原 0.1.1-rc.2）

## 这是什么
- 类型：DSH 插件（bundle，自研 · 未上传 GitHub）
- 一句话功能：主模型遇到专业/知识密集/不确定内容、用户 `@顾问群`、或同一问题重复 3 次仍未解决时，调用 `ask_advisors` 启动“顾问群”。顾问模型以**真实流式**返回思考过程与 Markdown 正文，在对话流中展示复古聊天群 UI 卡片；主模型可基于顾问意见继续追问，`maxRounds` 封顶总轮数。

## 来源
- 自研插件，无 git 远程仓库、未发布 npm。
- 代码位于 `I:\DSH\dsh-advisor-group`，基于 DSH 插件契约（Cordis）实现。

## 已实现功能

### 工具与触发
- `ask_advisors`：创建顾问会话、并行/串行调用顾问、每次调用只跑**一轮**（主模型问 → 顾问答一次）；跨轮讨论由主模型用 `sessionId + followUp` 驱动；`maxRounds` 作为总会话轮数上限，达到后提示主模型收尾。
- `toggle_advisor_group`：运行时启用/禁用，并**持久化到 settings**（重启不丢）。
- 触发机制（三层）：
  1. 用户消息 `@顾问群` / 点名“顾问群” → 绕过分类器强制启动；
  2. 同一问题重复 3 次仍未解决 → host 监听 `session/event` 统计重复次数，注入【强制升级】提示并绕过分类器；
  3. 主模型自评 `confidence`（0–1）低于阈值（默认 0.6）→ 分类器强制升级。
- 前置分类器：规则判断是否该启动顾问群、是否建议联网搜索。

### Provider 适配层
- 主通道：复用 DSH `ctx.llm` / `llm-pi-ai` 已配置的 Provider 与凭据。
- 兜底通道：`direct-http` 支持 OpenAI 兼容 / Anthropic / Gemini 三种协议直连，内置多个 Provider 预设（DeepSeek、月之暗面、阿里云百炼、OpenAI、Claude、Gemini、硅基流动、AIHubMix、OpenRouter 等，见 `src/providers/presets.ts`）。
- 两条通道均**真实流式**：逐 delta `publish` 到 SSE + 200ms 节流写入 durable session 日志。

### 流式与回放
- SSE 端点 `/advisor-group/stream?sessionId=...`，每条帧带 `id: bootId:seq` 与 JSON 中的 `eventId/bootId`。
- 服务端保留每会话最近 500 条、5 分钟 TTL 的环形回放缓冲；支持 SSE 标准 `Last-Event-ID` 头和 `lastEventId/bootId` 查询参数。
- 检测到 **服务重启（bootId 失配）** 或 **缓冲缺口** 时下发 `event: resync`，客户端重置 SSE 覆盖层，不静默丢消息。
- 客户端按 `eventId` 去重，避免重连/刷新导致文本重复。

### Durable Session 事件
- `advisor-group/start`、`advisor-group/message`、`advisor-group/delta`、`advisor-group/end`。
- 通过 `src/session-events-host.ts` 把事件名注册进 `KNOWN_SESSION_EVENT_TYPES`（当前 rc 的 workaround）。
- delta 事件带 `round` 字段；客户端按 `(advisorId, round)` 分桶，多轮不串泡。

### Client UI
- 复古 CRT 聊天群卡片：扫描线、消息气泡、标题状态（LIVE/DONE）、可展开的 💭 思考面板（**默认展开**，随思考过程自动滚底）。
- 顾问正文使用轻量 Markdown 渲染器 `src/client/markdown.ts`：标题、粗体/斜体、行内代码、围栏代码块、有序/无序列表、引用、链接、**表格**、分隔线；纯 React 文本节点输出，链接做协议白名单（拒绝 `javascript:`/`data:`/`vbscript:`）。
- 可视化设置页：“设置 → 插件 → 顾问群”，支持编辑开关、讨论轮数、单次最多顾问数、置信度阈值、顾问列表等。

### 安全与成本
- `/advisor-group/config` 读回配置时 **API key 掩码**（只保留末 4 位）；保存时回传掩码/空值则保留原 key，新明文 key 才覆盖。
- `/advisor-group/*` 路由带 **启动期共享 token** 鉴权：host 通过 `tapIndex` 注入 `globalThis.__ADVISOR_GROUP_TOKEN__`，客户端 fetch 带 `x-advisor-group-token` 头、SSE 带 `token` 查询参数；无 token 返回 401。
- 每日新咨询次数上限默认 50 次，`tryStartConsultation()` **原子 check-and-increment**（防并发穿透）；单顾问调用超时 120s；轮数上限 `maxRounds`。

### 主模型提示词注入
- 通过 `ctx.systemPrompt` 注入“顾问群使用边界”段落：何时调用、何时不调用、`context` 必须包含（a）当前项目大致情况（b）问题细节、未知写“该项背景未知”、拿到顾问结论后自行综合。

## 安装方法（开发期）
```sh
cd I:\DSH\dsh-advisor-group
npm install --legacy-peer-deps --no-audit --no-fund
npm run typecheck
npm test
npm run build
# 打包并安装到 web profile
npm pack --pack-destination .
dsh plugin --profile web remove dsh-advisor-group
dsh plugin --profile web add .\dsh-advisor-group-0.1.0.tgz
# 重启 DSH web 后浏览器硬刷新 Ctrl+Shift+R
```

## 验证
- `npm run typecheck`：通过
- `npm test`：**24 个单测全部通过**（分类器 4、客户端 round 匹配 3、Markdown 链接/表格/XSS 5、SSE 回放/resync 3、设置 API key reconcile 9）
- `npm run build`：产出 `lib/index.mjs` + `lib/client.js`，postbuild 校验 client bundle id 为双引号
- 端到端 RPC 实测：`standard` 会话触发 `ask_advisors`，事件序列 `start → delta(流式) → message(round=1) → end`；追问后 `main 追问消息 → message(round=2) → end`，顾问**不再连续自答**
- 当前 web profile 实机：`/advisor-group/config` 不带 token 返回 401，带 index 注入 token 返回 200 且 API key 掩码 `****vUuq`
- `dsh-tools` 单实例检查通过（无 AGENTS.md 11 章复发）

## 已知限制 / 后续项
- 路由鉴权是**共享 token**，不是多用户鉴权；若绑 `0.0.0.0` 或反代暴露到局域网，必须在外层加真正的鉴权。
- 每日计数为**内存态**，重启归零；严格成本控制建议改为落盘或按 token/金额计量。
- SSE 断连不会取消在途顾问调用（当前靠 120s 单顾问超时兜底）。
- 轻量 Markdown 渲染器不支持嵌套列表、内联 HTML、表格单元格内复杂块级内容；如需完整 CommonMark 可评估换 `markdown-it`/`marked` + 消毒。
- `stopOnConsensus` 设置项目前无实际作用（因轮次语义已改为“每 ask 一轮、主模型驱动追问”）。
- 分类器阈值 0.6 尚未用真实使用数据调优。

## 备注（坑、注意事项）
- **client bundle 禁止开 `minify: true`**：压缩器会把 banner 里的 `id: "dsh-advisor-group"` 改写成反引号模板字符串，`dsh-startup-guard` 的正则只认 `["']`，会误判“client bundle 未注册”并自动禁用插件。`tsdown.config.mjs` 已设 `minify: false`，构建后需检查 `lib/client.js` 中 id 为双引号格式。
- DSH 内置 `dsh-llm-pi-ai` 是多 Provider 适配器，优先复用；直连 HTTP 只是兜底。
- `KNOWN_SESSION_EVENT_TYPES` mutation 是当前 rc 的 workaround，不是官方 API；若未来有官方注册面请替换 `src/session-events-host.ts`。
- `turn`/`step` 从 session log 尾部推导，不是工具上下文直接暴露。
- `npm install` 用 pnpm 会因 rc 依赖范围失败，当前用 `npm install --legacy-peer-deps` 可装。
- 顾问模型配置示例见 `src/config.ts`；Provider 预设见 `src/providers/presets.ts`。
