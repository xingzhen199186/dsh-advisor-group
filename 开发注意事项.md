# dsh-advisor-group — 开发与修改注意事项

> 本文档记录本插件后续修改时必须遵守的约束与已踩过的坑。
> 修改前请先读这份文件，再读 `工程说明文件.md` 与 `tasks/todo.md`。

---

## 1. 构建与打包（最容易再踩的坑）

### 1.1 严禁开启 client bundle 的 `minify`
- **原因**：`tsdown.config.mjs` 的 client 构建如果开 `minify: true`，压缩器会把 banner 里的：
  ```js
  id: "dsh-advisor-group"
  ```
  改写成：
  ```js
  id:`dsh-advisor-group`
  ```
  而 `dsh-startup-guard` 的插件注册校验正则只认 `["']`，匹配不到反引号 → 误判“client bundle 未注册” → 自动禁用插件 → 浏览器请求 `client.js` 得到 404 → UI 报错横幅。
- **铁律**：`tsdown.config.mjs` 中 client 构建保持：
  ```js
  minify: false
  ```
- **每次构建后检查**：
  ```sh
  grep -n "id:" lib/client.js | head -1
  ```
  必须看到：
  ```js
  window.__ModuleLoader__.load({ id: "dsh-advisor-group", ...
  ```
  而不是反引号。

### 1.2 不要用 `link:` 安装到 profile
- 原因：`link:` 安装后，Node 从 `I:\DSH\dsh-advisor-group` 物理目录解析依赖，插件自己的 `node_modules` 里 rc 版本 peer 依赖不完整，会出现 `ERR_MODULE_NOT_FOUND`。
- 正确姿势：先构建，再 `npm pack` 得到 tarball，然后用：
  ```sh
  dsh plugin --profile web add I:\DSH\dsh-advisor-group\dsh-advisor-group-0.1.0.tgz
  ```
- 更新版本时：先 `dsh plugin --profile web remove dsh-advisor-group`，再加新 tarball。

### 1.3 npm install 的环境约束
- `pnpm install` 会因 rc 依赖版本范围失败（如 `@deepseek-ai/dsh-invariants`）。
- 当前可用：
  ```sh
  npm install --legacy-peer-deps --no-audit --no-fund
  ```
- 如果只想装依赖不触发构建：
  ```sh
  npm install --legacy-peer-deps --no-audit --no-fund --ignore-scripts
  ```

---

## 2. Client 模块声明（Settings Tab 可见性）

### 2.1 `dsh.client.inject` 不能为空
- 问题现象：设置 → 插件 里看不到“顾问群”标签页。
- 原因：client 模块没有声明对 settings UI 的依赖边，模块加载器没有正确组装。
- 当前（适配 `0.1.2-rc.1` 后）必须是：
  ```json
  "client": {
    "inject": [
      "@deepseek-ai/dsh-client-ui-renderer",
      "@deepseek-ai/dsh-client-ui-conversation",
      "@deepseek-ai/dsh-client-ui-chat",
      "@deepseek-ai/dsh-client-ui-settings-plugins",
      "@deepseek-ai/dsh-client-locale"
    ],
    "platform": "web"
  }
  ```
- 注意：`@deepseek-ai/dsh-client-runtime` 在 `0.1.2-rc.1` 已不存在（npm 最高版本 `0.1.1-rc.2`），不要写回该包；`set` 的名字也不能用 `settings.plugins.tab`（已改为 keyed `settings.plugin.item`，key=设置命名空间）。

### 2.2 设置页输入框样式
- 不要用 `var(--card)` / `var(--card-foreground)` 作为输入框背景/文字色，在 DSH 设置页会导致黑底黑字不可读。
- 正确参考写法：
  ```ts
  background: 'transparent',
  color: 'inherit',
  border: '1px solid var(--dsh-color-border, #3a3f4b)',
  ```
- 标签用：
  ```ts
  color: 'var(--dsh-color-muted, #8b90a0)'
  ```

### 2.3 `dsh-plugin-dev check` 的已知误报
- client 代码从 `@deepseek-ai/dsh-client-ui-conversation/client`、`@deepseek-ai/dsh-client-ui-chat/client` 等子路径导入类型时，`dsh-plugin-dev check` 会误报“缺少 peerDependency”。
- 实际 `peerDependencies` 已声明根包，可忽略该条失败。

---

## 3. 配置与持久化

### 3.1 可视化配置页
- 设置入口：DSH Web UI → 设置 → 插件 → **顾问群**（可收起/展开的插件卡片，2026-09-04 起与内置「网页搜索 / Bash」卡片同级；头部=标题+描述+箭头，含「未保存」标记，底部=放弃更改/保存设置）。
- 支持修改：启用开关、讨论轮数 `maxRounds`、单次最多顾问数 `maxAdvisorsPerCall`、置信度阈值 `confidenceThreshold`、顾问列表（名称/头像/供应商/模型/systemPrompt/temperature/maxTokens）。
- Host 路由（全部挂在 `/advisor-group` 前缀下，**统一走 token 鉴权**，见 8.1）：
  - `GET /advisor-group/config`：返回配置，**无任何 key 材料**——`apiKey` 恒为空串，仅返回 `apiKeyMetaByProvider`（每供应商 `{configured, last4}` 事实）供 SecretField 徽章显示。
  - `POST /advisor-group/config`：SecretField 协议——**空 apiKey = 保留原 key**；新明文 = 覆盖并入档；`clearApiKey: true` = 显式清除（并删除该供应商存档）；旧「精确掩码回传」仅作防御保留。
  - `GET /advisor-group/providers`、`GET|POST /advisor-group/models`、`GET|POST /advisor-group/test-connection`、`GET /advisor-group/stream`（SSE）。
- 持久化走 `ctx.settings` 的 `advisor-group` namespace，保存后立即更新内存配置。

### 3.2 工具必须读最新配置
- `ask_advisors` / `toggle_advisor_group` 不要保存旧的 `config` 闭包，执行时始终通过 `service.getConfig()` 读取。
- `toggle_advisor_group` 通过 `service.toggleEnabled()` 同时更新内存并 `settings.update` 持久化，新增配置读取代码也应按此规则。

### 3.3 主模型提示词
- 插件通过 `ctx.systemPrompt.section()` 注入 `advisor-group-boundary` 段落。
- 该段是 `text: (assembly) => buildAdvisorPrompt(service.getConfig(), service.getRepeatPressure(sessionId))`，每次组装时动态读取配置与重复问题压力。
- 修改提示词时保持简洁，避免占用过多系统提示词空间。

### 3.4 分类器影子模式（2026-09-05）
- 每次**非强制**（无 @顾问群、非重复 3 次）的 `ask_advisors` 分类判定后，向 `$DSH_HOME/storages/advisor-group/classifier-shadow.jsonl` 追加一条 JSONL 样本（问题截断 200 字符、置信度、判定、原因、是否启动）。
- 只读统计：`GET /advisor-group/shadow`（token 鉴权，返回最近 200 条 + total/escalated/launched/avgConfidence）。
- **纯观测**：不改变任何行为；用途是收集真实数据调优 `confidenceThreshold` 与规则集。修改样本格式后跑 `tests/shadow.test.ts`（3 用例）。

---

## 4. Durable Session 事件

### 4.1 自定义事件注册
- 插件使用四个自定义 session 事件：
  - `advisor-group/start`
  - `advisor-group/message`
  - `advisor-group/delta`
  - `advisor-group/end`
- 必须通过 `src/session-events-host.ts` 把事件名加入 `KNOWN_SESSION_EVENT_TYPES`，否则持久化层会拒绝未知事件。
- `0.1.2-rc.1` 的官方机制是信封上的 `SessionEvent.ignorable: true` 标记（`dsh-session-persistence` 读路径 `KNOWN_SESSION_EVENT_TYPES.has(type) || event.ignorable === true` 才放行），但 `Session.append()` 只对 surface 事件开放 opts，插件侧目前仍无法给自定义事件打标记，所以 **KNOWN Set 变异仍然是当前唯一可行通道**；若将来 append 暴露 ignorable 或提供官方注册面，立即替换 `session-events-host.ts`。

### 4.2 turn/step 推导
- `ToolRunContext` 不直接暴露 `turn/step`。
- `src/session-log.ts` 从 session log 尾部最近的 `step/start` / `turn/start` 推导，找不到则回退 `{ turn: 0, step: 0 }`。

### 4.3 delta 事件必须剔除 undefined
- `Session.append` 对 JSON 可序列化要求严格，`contentDelta: pendingText || undefined` 这类字段会导致 append 抛错。
- `src/session-log.ts` 的 `appendAdvisorDelta` 必须显式构造对象、只包含有值字段（历史 bug：线上手改修复过，源码曾回退，已合并）。

---

## 5. Provider 与模型调用

- 主通道：复用 DSH `ctx.llm` / `dsh-llm-pi-ai` 已配置的 Provider 与凭据。
- 兜底通道：`src/providers/direct-http.ts` 支持 OpenAI 兼容、Anthropic、Gemini 协议。
- **两条通道都必须流式**：`ctx-llm.ts` 的 `callViaCtxLlm` 和 `direct-http.ts` 的 `streamDirectHttp` 都通过 `onDelta` 回调吐增量；`service.callAdvisor` 统一做 `publish`（SSE）+ 200ms 节流落盘。
- 单顾问超时：`src/providers/timeout.ts` 当前 `ADVISOR_CALL_TIMEOUT_MS = 120_000`。
- Provider 预设：`src/providers/presets.ts` 中默认模型清单是占位值，真实接入时需按各平台最新模型修正。
- Anthropic 兼容独立选项：DeepSeek、月之暗面 Kimi、AIHubMix 已提供单独预设（如 `deepseek-anthropic`），选择后自动使用 Anthropic 协议与对应 baseURL。
- 阿里云百炼 Anthropic 兼容：支持按量付费、Token Plan、Coding Plan 三种独立预设；其中按量付费需把 `{WorkspaceId}` 替换为实际业务空间 ID，且三类套餐的 API Key 类型必须与 Base URL 配套使用。
- OpenRouter Anthropic 兼容：预设使用 `https://openrouter.ai/api`（Anthropic Skin），API Key 仍为 `OPENROUTER_API_KEY`，模型 ID 使用 `提供商/模型名` 格式。
- 硅基流动 Anthropic 兼容：预设使用 `https://api.siliconflow.cn/v1`（Anthropic Messages 端点），API Key 环境变量为 `SILICONFLOW_API_KEY`。

---

## 6. 轮次语义（重要，2026-09-05 已改为自动深挖流水线）

- **一次 `ask_advisors` 跑完整流水线**：`runAutoPipeline` 循环到 `maxRounds` 轮；每轮 =
  - （第 2 轮起）驱动模型先基于全部讨论生成**深入追问**（`generateDeepenQuestion`，以 main 消息入列、SSE/durable 同步展示）；
  - 顾问**按配置顺序接力**：A → B（看到 A）→ C（看到 A+B）……transcript 每接入一位重建一次（`runOneRound` 内 for...of，告别 Promise.all 并行；`discussion.parallel` 已废弃）。
- 每轮后 `hasReachedMaxRounds` 为真即停止，最后驱动模型生成**综合结论**（`generateConclusion`），作为 summary.conclusion 写入 end 事件并展示在卡片"📌 综合结论"段。
- **驱动模型来源**（`src/driver.ts` `resolveDriverSource`）：会话 `requestHeader().config{provider,model}`（＝当前 Agent 模型）→ 回退 `discussion.driverModel` → 回退静态文本（流水线永不因驱动失败中断；生成失败均降级）。
- 轮数计算沿用：`floor(advisor 消息数 / 顾问数)`（`getRoundCount`）；多顾问时不要用“消息条数 + 1”。
- `sessionId + followUp` 仍保留为**手动追加一轮**（单轮接力 + summarize，无 conclusion），同样受 `maxRounds` 封顶。
- `stopOnConsensus` 保持弃用（schema 兼容、UI 不展示）；`parallel` 同步弃用（2026-09-05）。
- **单顾问超时可配置**：`discussion.advisorTimeoutMs`（默认 120000ms，范围 1000-600000），service 读取后传入 ctx-llm / direct-http 两条通道（providers 内部统一 `withTimeout(timeoutMs ?? ADVISOR_CALL_TIMEOUT_MS, signal)`）；修改时勿回退成硬编码常量。

> 注意：流水线**单次工具调用耗时 = N × (驱动 + M 顾问)**，同步执行、SSE 全程展示；取消靠 `exec.signal` 贯通。修改轮次/接力逻辑后跑 `tests/driver.test.ts`（5 用例）与全量 64 测试。

---

## 7. SSE 流式与回放

- `src/stream-channel.ts` 维护每会话：`lastIds`（自增 seq）、`buffers`（最近 500 条、5 分钟 TTL）、`subscribers`。
- 每帧写 `id: <BOOT_ID>:<seq>`，JSON 带 `eventId` + `bootId`。
- `subscribe(sessionId, res, lastEventId, bootId)` 的回放规则：
  - `bootId` 与服务端 BOOT_ID 不一致 → 发 `event: resync`（重启）。
  - `lastEventId` 小于缓冲首帧 id - 1 → 发 `event: resync`（缺口）。
  - 否则回放 `id > lastEventId` 的缓冲帧。
- 客户端 `AdvisorGroupNodeView` 维护 `lastEventIdRef` / `lastBootIdRef`，收到 resync 后重置 live 覆盖层。
- **修改回放逻辑后必须补/跑 `tests/stream-channel.test.ts`**（3 条路径）。

---

## 8. 安全与成本护栏

### 8.1 路由 token 鉴权
- `registerAdvisorSettingsAndRoutes` 在 host 侧生成 `authToken`（`randomUUID`），通过 `webServer.tapIndex` 注入 `globalThis.__ADVISOR_GROUP_TOKEN__`。
- `handleConfigRequest` 校验请求头 `x-advisor-group-token` 或查询参数 `token`；不匹配返回 401。
- 客户端：`authHeaders()` 给 fetch 加头；SSE 用 `?token=...`。
- 这只是本机共享 token，**不是多用户鉴权**；若暴露局域网，必须在外面加反代鉴权。

### 8.2 API key 脱敏
- `maskApiKey`：**长度与原 key 一致**，例如 `sk-...1234` → `************1234`（只透出末 4 位）；短于等于 4 位时全部打星。
- `GET /advisor-group/config` 用 `sanitizeConfig` 掩码后返回；`apiKeysByProvider` 是服务端隐秘字段，**不返回给浏览器**。
- `POST` 用 `reconcileApiKeys`：**SecretField 语义（2026-09-05 起）**——`clearApiKey: true` 清除当前直接 key 并删除该供应商存档；同 scope 且空串/undefined → **保留**原 key；新明文 → 写入并入档；scope 变化且无新值 → 从 `apiKeysByProvider` 历史恢复（无则 undefined）；「精确掩码回传」仅作防御保留（旧协议）。注意：schema 中 `apiKey` 与 **`apiKeysByProvider` 均已标 `role('secret')`**（官方 `describe`/redactSecrets 红线——防止官方导出/同步/镜像面泄漏任何 key 材料，2026-09-05 新增 5 个红线单测锁定）；`clearApiKey` 为瞬态字段，reconcile 后剥离不落盘。
- `reconcileApiKeys` 同时维护服务端 `apiKeysByProvider`：切换供应商时把旧供应商 key 留档，切回旧供应商时自动恢复，避免 Key 跟随/丢失。
- **client 切换供应商（2026-09-05 SecretField 收敛）**：不再维护任何客户端 key 记忆（`keyMemory` 已删除）。GET 配置只返回 `apiKeyMetaByProvider`（configured/last4 事实），表单显示「已配置 ••••last4 / 未配置」徽章；API Key 输入框**恒为空起步**：留空保存 = 服务器保留原 key，输入新明文 = 覆盖，点「清除」= 发送 `clearApiKey: true` 显式删除（含存档）。服务端只接受当前 scope 的新明文；跨供应商切换时输入框重置为空（保留语义由历史恢复保证），因此不会把某供应商的 key 误写给另一个。
- direct-http 调用必须读**原始** `advisor.apiKey`，不能读 sanitize 后的配置副本。
- **诊断端点（models / test-connection）不能直接透传表单掩码**：请求体携带 `advisorId`（2026-09-04 起），服务端 `resolveDiagnosticApiKey` 按「顾问 id + 当前 credential scope」解析真实 key——入参为掩码（`isMaskedApiKey` 命中）或空串时回退到 `advisor.apiKey` / `apiKeysByProvider` 存档值；用户新键入的明文 key 永远优先。否则切换供应商后表单里只有掩码/空值，直接打真实供应商必然 HTTP 401（历史 bug：`新顾问 连接失败：HTTP 401`）。
- **诊断端点 SSRF 加固（2026-09-04 起）**：`assertSafeDiagnosticBase` 对**请求时客户端传入的 baseURL** 强制 https-only（或本机 loopback http）；https 下拒绝一切 **IP 字面量**（私网/环回/链路本地/元数据网段）与云元数据主机名；三个 query 函数 fetch 一律 `redirect: 'error'`（不跟随重定向）。已知边界：**DNS rebinding**（域名解析到内网地址）未做二次解析校验，单机本机场景记录为已知限制；改动后必须跑 `tests/settings-reconcile.test.ts` 的 `assertSafeDiagnosticBase` 6 个用例。

### 8.3 每日计数
- `service.tryStartConsultation()` 内完成“检查 + 递增”，必须保持单同步块（无 `await`），防止并发穿透。
- 当前 `MAX_DAILY_CONSULTATIONS = 50`，按 **UTC 日切**；**2026-09-04 起持久化到 `$DSH_HOME/storages/advisor-group/daily-guard.json`**（构造时加载、递增后串行原子写 tmp+rename，失败只降级持久性不影响内存计数）；重启**不再归零**。写失败仅 console.warn（不含任何 key），不要抛错阻断咨询。UI 展示剩余次数尚未实现。

---

## 9. Client 卡片与 Markdown

- 卡片状态来自 durable 事件；SSE `live` 覆盖层只在比 durable 内容更长时采用。
- `MessageBubble` 思考面板默认展开（`useState(false)`），更新时自动滚底。
- 顾问正文用 `src/client/markdown.ts` 的 `renderMarkdown`；**所有文本一律走 React 文本节点**（不要用 `dangerouslySetInnerHTML`）。
- 链接必须过 `sanitizeLinkUrl` 白名单（http/https/mailto/相对路径）；新增协议/规则后补 `tests/markdown.test.ts`。
- 表格渲染支持管道表格（header + separator + body），复杂表格/嵌套块级内容暂不支持。

---

## 10. 修改后验证清单

每次修改后至少执行：

```sh
cd I:\DSH\dsh-advisor-group
npm run typecheck
npm test
npm run build
```

然后检查：

```sh
# 1) client bundle id 必须是双引号
grep -o 'id: "dsh-advisor-group"' lib/client.js

# 2) 打包并安装到 profile
Remove-Item .\dsh-advisor-group-0.1.0.tgz -ErrorAction SilentlyContinue
npm pack --pack-destination .
dsh plugin --profile web remove dsh-advisor-group
dsh plugin --profile web add .\dsh-advisor-group-0.1.0.tgz
```

涉及 client 或 `dsh.client.inject` 改动后，**必须重启 DSH web 并硬刷新浏览器**，否则不生效。

---

## 11. 当前状态速览

- [x] Host：ask_advisors / toggle_advisor_group（持久化）/ classifier / 重复问题检测 / 每日 50 次护栏
- [x] 流式：ctx.llm + direct-http 双通道真流式；SSE eventId/bootId/resync 回放
- [x] 安全：API key 掩码、路由 token 鉴权
- [x] Client：复古聊天群卡片、思考面板默认展开+自动滚底、Markdown（含表格）、设置页
- [x] 测试：24 个单测（分类器 4 / round 匹配 3 / markdown 5 / SSE 3 / settings API key reconcile 9）
- [x] 实机：web profile 已重装重启，401/200 鉴权与掩码实测通过
- [ ] 剩余：`stopOnConsensus` 悬空处理、分类器阈值真实数据调优（知识库 X 留档已于 2026-09-04 完成）
